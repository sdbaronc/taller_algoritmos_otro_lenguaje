
  var n=prompt("digita un numero");
  var m=prompt("digita un numero");

  if (m>n)
  document.write("continuar el reprograma");

  else
  document.write("parar el programa");

  	for (num in rangen,m+1)
    document.write(num);